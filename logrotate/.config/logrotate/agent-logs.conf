# Agent log rotation — the Claude Code hook-approvals log is a plain append
# file with no built-in rotation. copytruncate because the writer keeps
# appending to the same path.
/home/sebastian/.claude/hook-approvals.log {
    weekly
    rotate 4
    compress
    missingok
    notifempty
    copytruncate
}
