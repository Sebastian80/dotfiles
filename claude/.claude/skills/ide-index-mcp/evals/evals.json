{
  "skill_name": "ide-index-mcp",
  "evals": [
    {
      "id": 1,
      "prompt": "I need to understand how the ExportHandler interface is used across this codebase. Find all implementations and every place it's referenced.",
      "expected_output": "Uses ide_find_class/ide_find_symbol to locate the type, ide_find_implementations for concrete implementations, ide_find_references for usages. Reports locations with semantic context. No Grep as primary mechanism. (Name is a stand-in — grade tool selection, not whether the symbol exists in the current project.)",
      "files": [],
      "expectations": [
        "Used ide_find_class or ide_find_symbol to locate the type (not Grep)",
        "Used ide_find_implementations to find concrete implementations (not Grep)",
        "Used ide_find_references to find all usages (not Grep)",
        "Did NOT use Grep or Bash grep/rg as primary search mechanism",
        "If the symbol does not exist, said so after an IDE-tool search — did not silently switch to Grep"
      ]
    },
    {
      "id": 2,
      "prompt": "Search for all TODO comments in the codebase, find all files named Dockerfile, and check where the setting batch_size appears in config or docs.",
      "expected_output": "Uses ide_search_text (with context='comments' for TODOs), ide_find_file for Dockerfile, and ide_search_text for batch_size across all file types. Does NOT fall back to Grep/Glob.",
      "files": [],
      "expectations": [
        "Used ide_search_text for TODO search (not Grep)",
        "Used ide_search_text with context='comments' for TODO filtering",
        "Used ide_find_file to find Dockerfile files (not Glob)",
        "Used ide_search_text for the batch_size search across all file types (not Grep)",
        "Did NOT fall back to Grep/Glob for any of the three searches"
      ]
    },
    {
      "id": 3,
      "prompt": "Show me the structure of the OrderService class, trace who calls its process method, and find all implementations of the interface it belongs to.",
      "expected_output": "Uses ide_find_class/ide_find_symbol to locate the class, ide_file_structure for its members, ide_call_hierarchy (direction=callers) for the method, ide_find_implementations for the interface — falling back to Grep only for language constructs the index cannot resolve (e.g. structural typing). (Name is a stand-in — grade tool selection.)",
      "files": [],
      "expectations": [
        "Used ide_find_class or ide_find_symbol to locate the class first",
        "Used ide_file_structure to show the class structure (not reading the whole file)",
        "Used ide_call_hierarchy with direction=callers for the method (not Grep)",
        "Used ide_find_implementations for the interface (Grep fallback only after identifying an index limitation, e.g. structural typing)",
        "Did NOT use Grep or Bash grep/rg as primary search mechanism"
      ]
    },
    {
      "id": 4,
      "prompt": "rename the method getPimField on the DataProviderDecorator class to getPimAttribute — every caller has to be updated, and make sure nothing still references the old name afterwards",
      "expected_output": "Locates the exact declaration position first (ide_find_class / ide_file_structure), runs ide_refactor_rename with file+line+column+newName (NOT symbol mode — rename does not support it), then verifies with Grep that no occurrence of getPimField remains and fixes stragglers with Edit.",
      "files": [],
      "expectations": [
        "Located the declaration position via ide_file_structure or ide_find_class before renaming (column on the method name)",
        "Used ide_refactor_rename with file+line+column (did not attempt language+symbol mode)",
        "Did NOT use sed/Bash for the rename",
        "Verified after the rename by grepping for the old name getPimField",
        "Fixed any leftover occurrences with Edit rather than rerunning the rename"
      ]
    },
    {
      "id": 5,
      "prompt": "I have shop-61 open in phpstorm. open /home/sebastian/workspace/shop-62.docker.local as well and tell me which class implements PriceProviderInterface in each of the two projects",
      "expected_output": "Checks ide_project_status first, opens the second project via ide_open_project (absolute path, waits for indexing), then runs ide_find_implementations twice — once per project — passing project_path on every call since multiple projects are now open.",
      "files": [],
      "expectations": [
        "Called ide_project_status (or equivalent) to see open projects before acting",
        "Used ide_open_project with the absolute path to open the second project",
        "Passed project_path on every subsequent tool call once two projects were open",
        "Used ide_find_implementations (not Grep) in both projects",
        "Did not ask the user to open the project manually in the IDE"
      ]
    },
    {
      "id": 6,
      "prompt": "where is the service oro_pricing.provider.combined_price_list defined and which PHP class is behind it? it's registered somewhere in a services.yml",
      "expected_output": "Does NOT rely on ide_find_definition on the YAML position (known provider-reference gap). Uses ide_search_text with the dotted service id and filePattern *.yml to find the definition block (or the JetBrains MCP Server's locate_symfony_service when connected), then resolves the class from the class: value.",
      "files": [],
      "expectations": [
        "Did not treat ide_find_definition returning the YAML node as the final answer",
        "Used ide_search_text with the service id and a *.yml filePattern (or locate_symfony_service on the jetbrains-mcp server)",
        "Resolved and reported the PHP class from the service definition block",
        "Consulted the symfony-di reference behavior instead of concluding the Symfony plugin is broken"
      ]
    },
    {
      "id": 7,
      "prompt": "I just ran a codemod script outside the IDE over the whole src/ tree that renamed applyDiscount to applyRebate. now find every remaining usage of applyDiscount — vendor code included",
      "expected_output": "Calls ide_sync_files first (external changes → stale index), then ide_find_references / ide_search_text with scope project_and_libraries so vendor/ is not silently skipped.",
      "files": [],
      "expectations": [
        "Called ide_sync_files before searching (external modifications mentioned)",
        "Used ide_find_references or ide_search_text (not Grep) as primary search",
        "Covered vendor code deliberately: scope project_and_libraries on find_* tools where a symbol anchor exists, and rg -uu for index-excluded vendor subtrees (ide_search_text has NO scope parameter)",
        "Distinguished exact-symbol matches from substring false positives (word-boundary discipline)"
      ]
    },
    {
      "id": 8,
      "prompt": "add a nullable ?LoggerInterface $logger parameter to CombinedPriceListsBuilder::build() — every call site has to keep working",
      "expected_output": "Uses ide_change_signature so the IDE updates all call sites atomically. Does NOT hand-edit the declaration and then hunt callers with grep.",
      "files": [],
      "expectations": [
        "Used ide_change_signature (not Edit, not ide_replace_text_in_file)",
        "Did NOT grep/rg for call sites afterwards — the refactoring updates them",
        "If the tool was unavailable or failed, said so rather than silently falling back to a manual edit plus a grep sweep"
      ]
    },
    {
      "id": 9,
      "prompt": "across the test suite, every ->getMock() call should become ->createMock(). don't touch the string 'getMock' where it appears in comments or docblocks",
      "expected_output": "Uses ide_structural_search_replace, which is AST-aware and will not match comments or string literals. A text-level replace is wrong here precisely because the prompt excludes comments.",
      "files": [],
      "expectations": [
        "Used ide_structural_search_replace",
        "Did NOT use a stream editor, ide_replace_text_in_file, or Edit for a project-wide code pattern",
        "Reasoning acknowledged that a text-level replace cannot honour the comment/docblock exclusion"
      ]
    },
    {
      "id": 10,
      "prompt": "run the unit tests for the pricing bundle and tell me what fails",
      "expected_output": "Uses ide_run_tests so the run goes through the IDE run configuration and its configured interpreter. On this machine the host php is 8.5 and breaks Magento/Oro test bootstraps, while the IDE interpreter points at the container — so invoking the host binary directly is the wrong instrument.",
      "files": [],
      "expectations": [
        "Used ide_run_tests, or explicitly chose a runner bound to the project interpreter (ddev exec / container php)",
        "Did NOT invoke the host php binary directly",
        "Reported actual failures from the run rather than predicting them from source"
      ]
    },
    {
      "id": 11,
      "prompt": "create a new empty PHP class Acme\\PricingBundle\\Util\\PriceRounder with a namespace declaration and nothing else in it",
      "expected_output": "A plain new file with no cross-file consequences. Claude's Write tool is the efficient choice; ide_create_file goes through the auto-mode classifier and buys nothing here. Either is defensible, but reaching for the gated tool without a reason is not.",
      "files": [],
      "expectations": [
        "Created the file (Write preferred; ide_create_file acceptable if justified)",
        "Did NOT use a semantic refactoring tool for what is a plain file creation",
        "If ide_create_file was used, gave a reason (e.g. wanting the IDE to index it immediately) rather than defaulting to it"
      ]
    },
    {
      "id": 12,
      "prompt": "find every usage of PriceListToCustomerRepository",
      "expected_output": "REGRESSION TEST for the 2026-07-26 false negative. Run with the target project NOT open in the IDE while a different project is. ide_find_symbol returns an empty result with stale:false — an authoritative-looking zero answered from the wrong project. The agent must not report 'no usages found'.",
      "files": [],
      "expectations": [
        "Did NOT report 'no usages' on the strength of an empty IDE result",
        "Checked ide_project_status (or otherwise verified the working directory is an open project) before trusting an empty result",
        "Either opened the project with ide_open_project, fell back to text search, or told the user the project is not open — any of these, but not silent acceptance of the empty",
        "Recognised that stale:false does not make an empty result authoritative"
      ],
      "precondition": {
        "automatable": true,
        "ide_projects_open": [
          "/home/sebastian/workspace/shop"
        ],
        "cwd": "/home/sebastian/workspace/magento-shop",
        "note": "Exactly one project open, and it is NOT the cwd. Set with ide_close_project / ide_open_project before the run."
      }
    },
    {
      "id": 13,
      "prompt": "who calls WebsiteSearchIndexer::reindex?",
      "expected_output": "Run with the IDE closed, so no ide_* tools exist in the session. The agent should use standard search tools without ceremony and must not ask the user to launch PhpStorm.",
      "files": [],
      "expectations": [
        "Answered using standard search tools",
        "Did NOT ask the user to start the IDE, or claim the answer would be better with it running",
        "Did not spend turns probing for IDE tools that are absent from the session"
      ],
      "precondition": {
        "automatable": false,
        "ide_running": false,
        "note": "Requires PhpStorm fully quit so no ide_* tools register. Cannot be set from inside a session - run by hand."
      }
    },
    {
      "id": 14,
      "prompt": "show me the file structure of PriceProvider.php",
      "expected_output": "Run with two or more projects open. The first call without project_path errors with multiple_projects_open and a candidate list. The agent should retry with the correct project_path, not abandon the IDE tools.",
      "files": [],
      "expectations": [
        "Retried the same IDE tool with project_path after the disambiguation error",
        "Did NOT fall back to text search because of the error",
        "Picked the project matching the working directory rather than guessing"
      ],
      "precondition": {
        "automatable": true,
        "ide_projects_open": [
          "/home/sebastian/workspace/shop",
          "/home/sebastian/workspace/magento-shop"
        ],
        "cwd": "/home/sebastian/workspace/magento-shop",
        "note": "Two projects open so the first project_path-less call errors with multiple_projects_open."
      }
    }
  ]
}
