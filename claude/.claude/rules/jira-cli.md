# Jira CLI Usage

- When piping `jira` CLI output to other tools, ALWAYS use `2>/dev/null` to suppress server startup messages. NEVER use `2>&1` as it merges startup noise into stdout and breaks JSON parsing.
- Prefer `--format ai` for reading ticket context, `--format json` only when programmatically extracting fields.
- Always use Jira wiki markup in comments and descriptions, never Markdown.
- To update issue fields (description, summary, etc.), use `jira update KEY --field value`. This is an alias for `PATCH /jira/issue/KEY`. Alternative: `jira issue KEY -X PATCH --field value`.
- Use dedicated endpoints for sub-resources: `jira comments KEY`, `jira attachments KEY`, `jira transitions KEY`. Don't parse the full issue JSON to extract comments or attachments.
- When parsing `--format json` output, fields are at `.data.fields`, NOT `.fields`. The CLI wraps responses in `{"success": true, "data": {...}}`.
- `jira search --jql '...'` JSON returns the issues array at `.data[]` directly, NOT `.data.issues[]`. The response shape is `{"success": true, "data": [...issues...], "pagination": {...}}`.
- `jira comment KEY --text "..."` — comment body flag is `--text`, NOT `--body`. The API field is `text`. Using `--body` errors with `"Field required: text"`.
- `--text` does NOT interpret `\n` escapes — they land literally in the comment. Pass real newlines (`--text "$(printf '...\n...')"`). `--custom` JSON values DO render `\n` as line breaks. A botched comment can be fixed: `jira comment KEY <id> -X DELETE`, then repost.
- To set system fields not on the `jira update KEY` / `jira issue KEY -X PATCH` direct flag list (e.g. `fixVersions`, which has no `--fixVersions` flag despite being a standard system field), use `--custom` with a JSON object: `jira issue KEY -X PATCH --custom '{"fixVersions":[{"name":"X"}]}'`. `--custom` accepts arbitrary JSON for any field, not just custom fields. The array replaces existing values. CAUTION: the `{"add":{...}}` update-operation form can silently no-op (observed on empty fixVersions — success response, nothing written); prefer the replace-array form and ALWAYS re-fetch to verify the value landed.
- Use dedicated commands for links (`jira link`) and sprints (`jira sprint`). Unknown flags on `jira issue` are silently ignored — the GET response looks like success.
- `jira transition KEY --target X` treats `X` as a destination STATE and does multi-step pathfinding that **executes** intermediate transitions. Pass the destination state (the `.to` value from `jira transitions KEY`), NEVER a transition name — a transition name path-walks the ticket destructively. There is no transition-by-id flag. Always `--dryRun --maxSteps 1` first to confirm a single expected hop.
- **After creating a ticket, ALWAYS add it to the project's active sprint.** Two steps: `jira sprint/active/{project}` to get the active sprint ID, then `jira sprint/{sprint_id}/issues [POST] --issues KEY`. Skip only if user explicitly says "backlog" or the project has no active sprint (cli will say so).
- `jira transitions KEY --format json`: `.data[].to` is a plain string (the state name), NOT an object — don't parse `.to.name`.
- Attachment **upload works** since plugin 3.5.1: `jira attachment KEY --file a.xml --file b.xml` (multi-file in one call, verified). There is still NO **download** endpoint; to read a screenshot's content, ask the user to describe it or paste it — don't curl the attachment URL (needs credentials Claude must not read).
- Multi-step transition pathfinding can report "No path" even when stepwise hops exist. Transition one hop at a time: `--target <next-state> --dryRun --maxSteps 1`, then execute, then re-list transitions.
- Versions: create via `jira version --project KEY --name "..." -X POST`; update via `jira version/{id} -X PATCH`, which supports ONLY `--name`/`--description`/`--released`. There is NO release-date parameter — `--releaseDate` is silently ignored, `--release_date` errors, `-X PUT` is Method Not Allowed. Release dates need the Jira UI.
- The Netresearch instance has NO *follows / is followed by* link type (check `jira linktypes`). Release-ticket chaining uses `Relation`; dependencies use `Blockade` (blocks / is blocked by).
- `jira comments KEY --format json` returns comments NEWEST-FIRST: `.data[0]` is the latest, `.data[-1]` the oldest. To verify a just-posted comment use `.data[0]` or `.data | sort_by(.created) | .[-1]` — checking `.data[-1]` makes a successful write look missing.
- `jira link --from A --to B --type Blockade` creates **B blocks A** (A "is blocked by" B) — the `--to` issue takes the outward "blocks" role. To express "A blocks B", call `--from B --to A`. Verify direction after creating: check `.inwardIssue`/`.outwardIssue` on the issue's links.
- Link field-vs-display trap (cost a UI-only-deletable wrong link on HMPM-977): `--from A --to B` stores the record with **B in the `inwardIssue` FIELD**, yet B's page DISPLAYS the outward phrase — the field name and the displayed role are inverted. Never replicate an existing link by copying which slot the other issue sits in; derive the call from the sentence you want. For type Cause (inward="is caused by", outward="causes"): "bug X causes customer ticket K" = `--from K --to X` (K then shows "is caused by X", matching the HMKG-2302→HMPM-977 reference).
- There is NO delete endpoint for issue links (only weblinks have DELETE; `jira link <id> -X DELETE` and raw `issueLink/<id>` return "Endpoint not found"). A wrongly-directed link can only be removed in the Jira UI — get the direction right the first time.
- Converting a standard issue to a Sub-task is UI-ONLY (Jira DC "Convert to Sub-task" wizard). `-X PATCH` with `{"issuetype":{...},"parent":{...}}` is rejected ("kein übergeordneter Vorgangsschlüssel angegeben") — the edit API refuses the `parent` field, with parent given by key OR id. Rejection is atomic (issue unchanged). Hand the user direct wizard links instead: `https://jira.netresearch.de/secure/ConvertIssueToSubTask!default.jspa?id=<numeric-issue-id>`.
- `jira show KEY --format json` returns a CURATED projection at `.data.issue.fields` that HIDES custom fields and `parent`. For raw fields (Epic Link, Sprint, parent, all customfields) use `jira issue KEY --format json` and read `.data.fields`. A sub-task looking parent-less in `jira show` output means nothing.
- `jira create --type` requires the issue-type NAME (e.g. `--type "Anforderung / User Story"`), NOT the numeric id — passing an id fails with "issue type is required". Watch for trailing spaces in type names (e.g. `"Change Request "`).
- When an MR/PR exists for a ticket, attach it as a WEBLINK (`jira weblink/KEY -X POST --url <mr-url> --title "MR !N: <mr title>"`), not only as a comment — comments scroll away, weblinks stay in the issue sidebar. Pattern: weblink for the MR itself + comment for evidence (test results, review verdicts). Verify with `jira weblinks/KEY`.
- UAT INSTRUCTIONS go into the dedicated `UAT` field (`customfield_10071`, format `h4. UAT` / `h5. Voraussetzungen` + case table, see DHLGW-1550), NOT into comments; only UAT *results/evidence* are comments. Ignore `customfield_11489` "User Acceptance Tests" — it's an unused Development/Staging template. Set via `jira issue KEY -X PATCH --custom '{"customfield_10071":"..."}'`.
- The UAT field is NOT on every issue type's screen — writing it then fails with `Field 'customfield_10071' cannot be set. It is not on the appropriate screen, or unknown.` In DHLGKP, `Technical task` (subtask:false) HAS the field, `Neue Aufgabe` does NOT. Check a project's types incl. subtask flag via `jira project/KEY | jq '.data.issueTypes[] | "\(.name) | subtask: \(.subtask)"'`.
- NEVER change the issue type via API. `-X PATCH --custom '{"issuetype":{...}}'` technically succeeds for standard→standard types, but it does NOT migrate the workflow step — the issue is left on an orphaned state showing transitions from the wrong workflow (telltale: a self-transition to the CURRENT status is offered, e.g. "Resume work -> In Arbeit" while already In Arbeit), and users clicking the broken buttons can strand the issue in a forward-only state with no path back (learned the hard way on DHLGKP-388/389). ANY type change goes through the UI Move wizard (`https://jira.netresearch.de/secure/MoveIssue!default.jspa?id=<numeric-id>`), which forces a proper status mapping — that wizard is also the repair for an already-stranded issue (move to the same type, remap the status).
